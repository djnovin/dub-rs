[package]
name = "dub"
version = "0.1.0"
edition = "2021"

[dependencies]
dub-core = { path = "../dub-core" }
dub-api = { path = "../dub-api" }
serde_json = "1.0"

[dev-dependencies]
tokio = { workspace = true }
chrono = { workspace = true }
wiremock = "0.6"
